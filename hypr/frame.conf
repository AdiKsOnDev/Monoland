# Companion to the quickshell screen frame (quickshell/services/Frame.qml).
# The frame bands reserve the outer margin, so outer gaps are handled there.
general {
    gaps_out = 0
    gaps_in = 8
    border_size = 0
}

decoration {
    rounding = 18   # == Frame.radius; window corners mate with the concave fillets
}
